{
  "name": "altrugenix-monorepo",
  "version": "1.0.0",
  "license": "MIT",
  "type": "module",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "yarn workspaces foreach -A run build",
    "dev:ui": "yarn workspace @altrugenix/ui dev",
    "build:lib": "yarn workspace @altrugenix/ui build",
    "dev:docs": "yarn workspace altrugenix-docs dev",
    "build:docs": "yarn workspace altrugenix-docs build",
    "build:storybook": "yarn workspace @altrugenix/ui build-storybook",
    "dev:storybook": "yarn workspace @altrugenix/ui storybook",
    "lint": "yarn workspaces foreach -A run lint",
    "test": "yarn workspaces foreach -A run test",
    "format": "prettier --write \"**/*.{js,jsx,ts,tsx,json,md}\"",
    "build:all": "yarn build:lib && yarn build:docs && yarn build:storybook",
    "change": "changeset",
    "version:ci": "changeset version",
    "release": "changeset publish",
    "release:github": "changeset publish"
  },
  "dependencies": {
    "prettier": "^3.8.3"
  },
  "devDependencies": {
    "@changesets/cli": "^2.31.0",
    "@eslint/js": "^10.0.1",
    "@tailwindcss/postcss": "^4.2.4",
    "@tailwindcss/vite": "^4.2.4",
    "@vitejs/plugin-react": "^6.0.1",
    "eslint": "^10.2.1",
    "eslint-config-prettier": "^10.1.8",
    "eslint-plugin-react-hooks": "^7.1.1",
    "eslint-plugin-react-refresh": "^0.5.2",
    "globals": "^17.5.0",
    "jsdom": "^29.1.0",
    "typescript": "^6.0.3",
    "typescript-eslint": "^8.59.1",
    "vite": "^8.0.10",
    "vite-plugin-dts": "^4.5.4",
    "vitest": "^4.1.5"
  },
  "packageManager": "yarn@4.13.0"
}
